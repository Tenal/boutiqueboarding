import React from "react";
import { Typography } from "@mui/material";

function RouteManager() {
    return (
        <div>
            <Typography variant="body1">Hello, React App!</Typography>
        </div>
    );
}

export default RouteManager;
